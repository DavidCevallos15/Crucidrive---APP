'use client'

export default function SosButton() {
  return (
    <button
      type="button"
      className="sonar press relative z-[1000] flex size-14 items-center justify-center rounded-full bg-danger text-sm font-bold tracking-wider text-foreground shadow-lg shadow-danger/40"
      aria-label="Botón de pánico SOS"
    >
      SOS
    </button>
  )
}
